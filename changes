* major rewrite with new structure
* change the language and translations model from meta to taxonomy (don't use the termmeta table anymore)
* move the strings translations from option to post type
* add support of wordpress importer plugin. Export must have been done with Polylang 1.2+
* add support for language code in subdomain
* better management of default category for each language
* update management of the 'copy' action of the custom fields section in wpml-config.xml
* start improving the inline doc

1.2dev42
* suppress the 'Posts' custom post type in Polylang settings
* Add some descriptions in Polylang settings and strings translations
* Add the possibility to display the upgrade notice on plugins page (included in 1.1.6)
* Bug correction (included in 1.1.6): untranslated post types are auto translated
* improve inline doc
* correct syntax errors for PHP < 5.4

1.2dev43
* Bug correction (included in 1.1.6): tags are not added to post when the name exists in several languages and they are not translations of each other
* improve inline doc

1.2dev44
* Request confirmation for deleting a language
* Modify admin language filter for valid html
* update .pot file
* Improve inline doc

1.2dev45
* fix: notice when a language has been deleted
* fix: API, WPML compat and plugins compat not loaded
* fix: custom flags not loaded

1.2dev46
* Add support for one domain per language
* fix: notices if upgrade from version < 1.0
* fix: css bug in chrome

1.2dev47
* Add support for ICL_LANGUAGE_CODE and ICL_LANGUAGE_NAME on admin side
* fix: strings translation not deleted in db when deleting a language
* fix: strings translations not saved if one has slashes
* Improve inline doc

1.2dev48
* fix: bad redirection if multiple domains and not hiding language code for default language
* fix: bad archive url for multiple domains
* update .pot file

1.2dev49
* Add support for theme navigation customizer
* fix: bad links when setting language from content when using permalinks

1.2dev50
* add support of WPSEO custom strings translations when the language is set from content
* fix: wpml-config.xml does not work
* fix typo in one string

1.2dev51
* now check if date and post type archives are translated before displaying the language switcher
* fix broken site if WPSEO not active

1.2dev52
* fix: language name (instead of term name) displayed on terms archive pages

1.2dev53
* Include rel="alternate" hreflang="x" selflink per google recommendation
* Bug correction: same id is used for all language items in menu
* fix: PHP warning if menu locations have not been filled yet

1.2dev54
* Synchronization is now disabled by default (due to too much conflicts / questions on the forum)
* add the option 'no_default_cat' optional argument to PLL_Admin_Model::add_language
* remove static var $languages in PLL_Model::get_languages_list
* make sure the default category is assigned the new default language if the default language is deleted
* fix: Fatal error: Call to undefined method stdClass::set_flag()
* fix: Polylang notice in icl_get_languages
* fix: current language is not selected in dropdown language switcher
* fix: notices in nav_menus upgrade
* fix: 404 error on pages introduced in 1.2dev52

1.2dev55
* move pre_get_posts filters to parse_query filters
* Bug correction: wpml-config.xml file not loaded for sitewide active plugins on network installations
* fix: ICL_LANGUAGE_CODE and ICL_LANGUAGE_NAME not correctly set on admin side at language switch
* fix: backward compatibility debug message does not display file and line on old versions of PHP (PHP < ???)
* fix: various PHP notices and warnings
* fix: decreased priority of links filters to 20 -> compatibility with wp-ecommerce page links (was OK in 1.1.x)

1.2dev56
* Add Arabic translation
* PLL_Filters_Base class removed, PLL_Links created
* Bug correction: page parent dropdown list (in page attributes metabox) not correctly displayed when switching from a language with empty list
* replace usage of action dbx_post_advanced (deprecated since WP 3.7) by add_meta_boxes
* fix: 404 error on pages again!
* fix: text direction on admin side according to language setting

1.2dev57
* reworked again parse_query filters
* fix: PHP Notice: Undefined index in /wp-admin/nav-menus.php when Polylang is de-activated
* fix: update post count in languages after import
